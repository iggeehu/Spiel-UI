//Wrapper for constants
import { PixelRatio } from 'react-native';

export const CONSTANTS = {
    SCREEN_WIDTH: PixelRatio.getPixelSizeForLayoutSize(390),
    SCREEN_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(844),
    STATUS_BAR_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(47),
    UPPER_NAV_BAR_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(44),
    REVIEW_CARD_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(668),
    REVIEW_BODY_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(497),
    NAV_BAR_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(57),
    IMAGE_HEIGHT : PixelRatio.getPixelSizeForLayoutSize(342),
    IMAGE_WIDTH : PixelRatio.getPixelSizeForLayoutSize(342),
    REVIEW_META_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(83),
    USER_INFO_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(48),
    LIKER_PROFILE_HEIGHT: PixelRatio.getPixelSizeForLayoutSize(32),
    UNIVERSAL_LEFT_MARGIN: PixelRatio.getPixelSizeForLayoutSize(24),
  }

